{{#if skills}}
<section id="skills">
    <h1 class="title">Habilidades</h1>
    <div class="content">
      {{#each skills}}
      <ul class="skill-list">
        {{#each this}}
        <li>
          <span>{{this.title}} - {{this.level}}</span>
        </li>
        {{/each}}
      </ul>
      {{/each}}
    </div>
</section>
{{/if}}
